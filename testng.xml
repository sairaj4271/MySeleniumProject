<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd" >
<suite name="Bluecopa Suite" parallel="classes" thread-count="3">

    <!-- Global parameters available to all test classes -->
    <parameter name="baseUrl" value="https://showcase.bluecopa.com"/>
    <parameter name="username" value="auto-testadmin@bluecopa.com"/>
    <parameter name="password" value="Admin@copa123"/>

     <listeners>
        <listener class-name="listeners.TestListener"/>
    </listeners>
    <listeners>
   <listener class-name="listeners.TestListener"/>
</listeners>

    <test name="UI Tests">
        <packages>
            <package name="test"/>
        </packages>
    </test>

</suite>
